#!/bin/sh
# Drop IDE/agent footers from commit messages (local policy).
msg_file=$1
test -f "$msg_file" || exit 0
grep -vE '^Made-with:|^Co-authored-by:.*[Cc]ursor' "$msg_file" >"${msg_file}.tmp" && mv "${msg_file}.tmp" "$msg_file"
exit 0
