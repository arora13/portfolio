#!/bin/sh
# Remove Cursor "Made-with:" trailer so commit messages stay clean on GitHub.
msg_file=$1
test -f "$msg_file" || exit 0
grep -v '^Made-with:' "$msg_file" >"${msg_file}.tmp" && mv "${msg_file}.tmp" "$msg_file"
exit 0
